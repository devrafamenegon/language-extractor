/**
 * Exemplo com múltiplos tipos de erros
 */
int main() {
    int x = 10;
    int y = z + 5;  // Erro semântico: 'z' não declarado
    
    if (x > 5 {  // Erro sintático: parêntese não fechado
        char* msg = "String não fechada
        int arr[10;  // Erro sintático: colchete não fechado
    }
    
    return 0;
}

